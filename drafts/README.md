# 自动草稿区

本目录只存放 C 级自动草稿和研究证据包，不代表项目正式发布的教程。

规则：

- 草稿必须使用 `verification: automated-draft`；
- 草稿必须使用 `status: draft`；
- 正式发布前必须完成人工审核；
- 移入 `docs/` 前必须升级为 `source-verified` 或 `tested`；
- 不得在草稿中包含密钥、个人信息、原始日志或未授权全文。

研究证据包建议放在：

```text
drafts/research-packs/<slug>.yaml
```
